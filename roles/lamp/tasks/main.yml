---
# tasks file for lamp
# Installation Apache
- name: Installation Apache
  apt:
    name:
      - apache2
      - libapache2-mod-php

- name: Installation PHP
  apt:
    name:
      - php

- name: Installation MySQL
  apt:
    name:
      - mysql-server
      - python-mysqldb

- name: Modification du fichier vhost par défaut Apache 
  template:
    src: vhost.conf.j2
    dest: /etc/apache/sites-available/000-default.conf
